require 'test_helper'

class DocumentCaptureRequirementsTest < ActiveSupport::TestCase

  test '5.8.1 (O)' do
    # There must be functionality for capturing electronic documents
    # independently of file format, methods for technical coding, sources or
    # other technical characteristics.
    NOT_YET_IMPLEMENTED
  end

  test '5.8.2 (O)' do
    # Data capture must take place in such a way that the document's appearance
    # (layout integrity) is maintained.
    NOT_YET_IMPLEMENTED
  end

  test '5.8.3 (V)' do
    # Data capture must take place in such a way that the document's content
    # integrity is maintained.
    NOT_YET_IMPLEMENTED
  end

  test '5.8.4 (V)' do
    # There should be functionality for fully automatic document capture.
    NOT_YET_IMPLEMENTED
  end

  test '5.8.5 (B)' do
    # In connection with fully automatic document capture, it must be possible
    # to link all obligatory metadata to the document.
    NOT_YET_IMPLEMENTED
  end

  test '5.8.6 (B)' do
    # In connection with fully automatic document capture, it must be possible
    # to link documents to a classification system.
    NOT_YET_IMPLEMENTED
  end

  test '5.8.7 (V)' do
    # In connection with fully automatic document capture, it should be possible
    # to link documents to one or several files or classes in the record
    # structure.
    NOT_YET_IMPLEMENTED
  end

  test '5.8.8 (O)' do
    # There must be no restrictions on the number of documents that can be
    # linked to a class or file.
    NOT_YET_IMPLEMENTED
  end

  test '5.8.9 (O)' do
    # There must be no restrictions on the number of documents that can be
    # archived in the solution.
    NOT_YET_IMPLEMENTED
  end

  test '5.8.10 (O)' do
    # There must be functions for ensuring that all components in a composite
    # document are captured.
    NOT_YET_IMPLEMENTED
  end

  test '5.8.11 (O)' do
    # There must be functions for ensuring that a composite electronic document
    # is handled as an entity, in which the relationship between the components
    # and the document's internal structure is maintained.
    NOT_YET_IMPLEMENTED
  end
end
